{
  "name": "geordi-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:exports": "pnpm build && node scripts/smoke-package-exports.mjs",
    "test:docs": "node scripts/check-doc-hygiene.mjs",
    "test:package-names": "node scripts/check-package-name-scope.mjs",
    "test:placeholders": "node scripts/check-test-placeholders.mjs",
    "test:render-everywhere:bunny": "pnpm --filter @flyingrobots/geordi-render-fixture test && pnpm --filter @flyingrobots/geordi-example-browser-render-everywhere test && cargo test -p native-render-everywhere && cargo clippy -p native-render-everywhere --all-targets -- -D warnings && cargo run -p native-render-everywhere -- --bunny-check fixtures/render-everywhere/assets/stanford-bunny && cargo run -p native-render-everywhere -- --bunny-smoke fixtures/render-everywhere/assets/stanford-bunny && cargo run -p native-render-everywhere -- --bunny-smoke --frame 15 fixtures/render-everywhere/assets/stanford-bunny && cargo run -p native-render-everywhere -- --bunny-smoke --frame 60 fixtures/render-everywhere/assets/stanford-bunny",
    "test:render-everywhere:gpvue": "pnpm --filter @flyingrobots/geordi-core build && pnpm --filter @flyingrobots/geordi-render-fixture build && pnpm --filter @flyingrobots/geordi-gpvue build && node --experimental-strip-types scripts/render-everywhere-gpvue-smoke.ts",
    "test:repo-sludge": "node scripts/check-repo-sludge.mjs",
    "test:coverage": "turbo run test:coverage",
    "lint": "pnpm lint:root && turbo run lint",
    "lint:root": "eslint . --max-warnings 0",
    "typecheck": "turbo run typecheck",
    "wesley": "wesley",
    "clean": "turbo run clean && rm -rf node_modules",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@flyingrobots/geordi-core": "workspace:*",
    "@flyingrobots/geordi-gpvue": "workspace:*",
    "@flyingrobots/geordi-render-fixture": "workspace:*",
    "@types/node": "^25.9.1",
    "@typescript-eslint/eslint-plugin": "^8.60.1",
    "@typescript-eslint/parser": "^8.60.1",
    "eslint": "^10.4.1",
    "eslint-config-prettier": "^10.1.8",
    "turbo": "^2.9.16",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0"
    }
  }
}
